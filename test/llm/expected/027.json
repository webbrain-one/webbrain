{
  "id": "027",
  "idealFirstToolCall": {
    "name": "read_page",
    "args": {}
  },
  "successRubric": "Reads the current page and emits a short summary via done(). Calling fetch_url on the same URL would be wrong (we're already on the page)."
}
